# ─── Required ────────────────────────────────────────────
# OpenAI API key for GPT-4o-mini AI summarization
OPENAI_API_KEY=sk-your-openai-api-key

# PostgreSQL connection string
# Local dev:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/war_panel
# Production (self-hosted Postgres on your VPS):
# DATABASE_URL=postgresql://war_panel_user:strongpassword@localhost:5432/war_panel

# ─── Optional ────────────────────────────────────────────
# RSS.app Premium API credentials (manages Telegram/OSINT feeds)
# Create feeds in the RSS.app dashboard; the server fetches them all automatically
RSSAPP_API_KEY=
RSSAPP_API_SECRET=

# Proxy server base URL for Pikud HaOref API (needed from non-Israeli IPs)
PROXY_BASE_URL=
PROXY_AUTH_TOKEN=

# Server port (default: 5000)
PORT=5000

# Task #8: MarineTraffic
MARINETRAFFIC_API_KEY=

# Task #17: ADS-B Exchange
ADSBX_API_KEY=

# Task #6: Sentinel Hub
# Legacy API key (PLAK...) — works without account upgrade
SENTINELHUB_API_KEY=
SENTINELHUB_INSTANCE_ID=
# OAuth2 credentials (optional — enables Catalog API for better imagery search)
SENTINELHUB_CLIENT_ID=
SENTINELHUB_CLIENT_SECRET=
